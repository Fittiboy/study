# Study
A CLI to keep track of my studying pipeline.

# Build
`zig build --release=small` for a small binary called `study` to be placed in `[project root]/zig-out/bin`.

Requires Zig version 0.16.0. Some 0.17.0-dev versions work as well, but this is tested with 0.16.0.
